name: rpi_eyes
description: "ST7789 SPI display driver for Raspberry Pi with Flutter UI rendering."
publish_to: 'none'
version: 1.0.2

environment:
  sdk: ^3.10.8

dependencies:
  flutter:
    sdk: flutter
  dart_periphery: ^0.9.20
  web_socket_channel: ^3.0.3
  window_manager: ^0.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
